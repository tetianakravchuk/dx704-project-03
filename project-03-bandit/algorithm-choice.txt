UCB1 (upper confidence bound) for bounded nonnegative rewards.
Rewards are bounded continuous (not 0/1), so UCB1 applies; Beta-Bernoulli Thompson assumes Bernoulli rewards and ε-greedy is a weaker baseline.
